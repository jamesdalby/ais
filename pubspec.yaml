name: aisdecode
description: Decode AIS messages
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  nmea:
    git:
      url: https://github.com/jamesdalby/nmea.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
